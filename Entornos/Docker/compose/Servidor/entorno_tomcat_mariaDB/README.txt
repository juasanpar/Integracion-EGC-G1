Entorno docker Tomcat con MariaDB.

Añadir scripts que se necesiten en la BBDD en la carpeta /db_dump.

Añadir el war de la aplicación que se quiera en la carpeta /war.

Es necesario que la aplicación (WAR) que se añada, apunte a la BBDD mediante la siguiente URL
mysql_tomcat_dp:3306